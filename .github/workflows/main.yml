name: Gauguin Kernel Build (Android 10)

on:
  workflow_dispatch:
    inputs:
      kernel_branch:
        description: 'Kernel branch'
        required: true
        default: 'gauguin-q-oss'
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    
    env:
      ARCH: arm64
      SUBARCH: arm64
      CROSS_COMPILE: aarch64-linux-android-
      KERNEL_DEFCONFIG: gauguin_defconfig
      KERNEL_DIR: ${{ github.workspace }}/kernel
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Display System Info
        run: |
          echo "=== System Information ==="
          uname -a
          nproc
          free -h
          echo "=========================="

      - name: Install Build Dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            bc \
            bison \
            flex \
            libc6-dev \
            libncurses5-dev \
            libssl-dev \
            libelf-dev \
            gcc \
            g++ \
            make \
            git \
            wget \
            curl \
            python3 \
            ccache
          
          echo "=== Installed Tools ==="
          gcc --version
          make --version
          bison --version
          flex --version

      - name: Setup ccache
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          key: gauguin-kernel
          max-size: 2G

      - name: Download Android GCC 4.9 Toolchain
        run: |
          echo "Downloading GCC 4.9 toolchain for aarch64..."
          mkdir -p ${{ github.workspace }}/toolchain
          cd ${{ github.workspace }}/toolchain
          
          # 尝试使用 Google 官方 Android 工具链，如果失败则使用备份
          git clone --depth=1 https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9 \
            || (wget -q https://github.com/arter97/arm64-gcc/releases/download/20221216/arm64-gcc-12.1-20221216.tar.xz && \
                tar -xf arm64-gcc-12.1-20221216.tar.xz)
          
          echo "${{ github.workspace }}/toolchain/aarch64-linux-android-4.9/bin" >> $GITHUB_PATH
          echo "${{ github.workspace }}/toolchain/arm64-gcc-12.1-20221216/bin" >> $GITHUB_PATH
          
          echo "=== Toolchain Path Updated ==="
          which aarch64-linux-android-gcc || which aarch64-linux-gnu-gcc
          (aarch64-linux-android-gcc --version || aarch64-linux-gnu-gcc --version) || echo "Toolchain info not available"

      - name: Clone Kernel Source
        run: |
          echo "Cloning gauguin kernel source..."
          git clone --depth=1 \
            -b ${{ github.event.inputs.kernel_branch || 'gauguin-q-oss' }} \
            https://github.com/MiCode/Xiaomi_Kernel_OpenSource.git \
            ${{ env.KERNEL_DIR }}
          
          cd ${{ env.KERNEL_DIR }}
          echo "Kernel source cloned successfully"
          git log --oneline -5

      - name: Verify Kernel Config
        run: |
          cd ${{ env.KERNEL_DIR }}
          echo "=== Available Configs ==="
          find arch/arm64/configs/ -name "*gauguin*" -type f || echo "Searching for defconfigs..."
          ls arch/arm64/configs/ | grep -i gauguin || true

      - name: Configure Kernel
        run: |
          cd ${{ env.KERNEL_DIR }}
          export PATH="/usr/lib/ccache:$PATH"
          
          echo "Configuring with ${{ env.KERNEL_DEFCONFIG }}..."
          make O=out ${{ env.KERNEL_DEFCONFIG }}
          
          echo "=== Kernel Config Generated ==="
          ls -lh out/.config
          head -30 out/.config

      - name: Build Kernel
        run: |
          cd ${{ env.KERNEL_DIR }}
          export PATH="/usr/lib/ccache:$PATH"
          
          echo "Starting kernel build with $(nproc) CPU cores..."
          make -j$(nproc) O=out 2>&1 | tee build.log
          
          echo "Build completed"
          echo "=== Last 50 lines of build log ==="
          tail -50 build.log

      - name: Check Build Output
        if: success()
        run: |
          cd ${{ env.KERNEL_DIR }}
          echo "=== Build Artifacts ==="
          ls -lh out/arch/arm64/boot/
          
          # 检查关键文件
          if [ -f out/arch/arm64/boot/Image ]; then
            echo "✓ Kernel Image found"
            file out/arch/arm64/boot/Image
            ls -lh out/arch/arm64/boot/Image
          fi
          
          if [ -f out/arch/arm64/boot/Image.gz ]; then
            echo "✓ Kernel Image.gz found"
            file out/arch/arm64/boot/Image.gz
            ls -lh out/arch/arm64/boot/Image.gz
          fi

      - name: Upload Kernel Image
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: gauguin-kernel-image
          path: ${{ env.KERNEL_DIR }}/out/arch/arm64/boot/Image*
          retention-days: 30

      - name: Upload DTB Files (if exists)
        if: success()
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: gauguin-kernel-dtb
          path: ${{ env.KERNEL_DIR }}/out/arch/arm64/boot/dts/
          retention-days: 30

      - name: Upload Build Log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log-${{ github.run_number }}
          path: ${{ env.KERNEL_DIR }}/build.log
          retention-days: 7

      - name: Create Release (on Tag)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: |
            ${{ env.KERNEL_DIR }}/out/arch/arm64/boot/Image
            ${{ env.KERNEL_DIR }}/out/arch/arm64/boot/Image.gz
          body: |
            Gauguin Kernel Build
            Branch: ${{ github.event.inputs.kernel_branch || 'gauguin-q-oss' }}
            Commit: ${{ github.sha }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Summary
        if: always()
        run: |
          echo "==============================="
          echo "=== Build Summary ==="
          echo "Build Status: ${{ job.status }}"
          echo "Kernel Branch: ${{ github.event.inputs.kernel_branch || 'gauguin-q-oss' }}"
          echo "Architecture: ${{ env.ARCH }}"
          echo "Defconfig: ${{ env.KERNEL_DEFCONFIG }}"
          echo "GitHub Actions Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          echo "==============================="
